form.form-row
  .col-12
    .form-group
      label Ejemplo de Input de rango:
      input.form-control-range(type='range')
    .form-group
      label Ejemplo de Input de rango deshabilitado:
      input.form-control-range(type='range', disabled)
  .col-12
    .form-group
      label Ejemplo de Input de rango custom:
      input.custom-range(type='range')
    .form-group
      label Ejemplo de Input de rango custom deshabilitado:
      input.custom-range(type='range', disabled)
