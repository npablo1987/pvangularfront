form.form-row
  .col-12
    .form-group
      label Ejemplo de Input de archivo:
      input.form-control-file(type='file')
    .form-group
      label Ejemplo de Input de archivo deshabilitado:
      input.form-control-file(type='file', disabled)
  .col-12
    .form-group
      label Ejemplo de Input de archivo custom:
      .custom-file
        input#customFileExample.custom-file-input(type='file')
        label.custom-file-label(
          for='customFileExample',
          data-browse='Seleccionar'
        )
          | Escoge un archivo
    .form-group
      label Ejemplo de Input de archivo custom deshabilitado:
      .custom-file
        input#customFileExampleDisabled.custom-file-input(type='file', disabled)
        label.custom-file-label(
          for='customFileExampleDisabled',
          data-browse='Seleccionar'
        )
          | Escoge un archivo
