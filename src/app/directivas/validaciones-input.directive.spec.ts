import { ValidacionesInputDirective } from './validaciones-input.directive';

describe('ValidacionesInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionesInputDirective();
    expect(directive).toBeTruthy();
  });
});
